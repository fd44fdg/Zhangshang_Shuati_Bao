# 数据库# 数据库配置
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=zhangshang_shuati
DB_PORT=3306

# JWT 密钥配置
# 强烈建议使用一个长且随机的字符串作为密钥，例如通过 `openssl rand -base64 32` 生成
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=7d

# 服务器运行端口
PORT=3000配置
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=zhangshang_shuati
DB_PORT=3306

# JWT 密钥配置
# 强烈建议使用一个长且随机的字符串作为密钥，例如通过 `openssl rand -base64 32` 生成
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=7d

# 服务器运行端口
PORT=3000